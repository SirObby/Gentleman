/* stdio.h has printf, automatically included. */

int get_number = (/* Arguments */) {
    return(14);
}

int main = (/* Arguments */) {

    printf ("Hello World, Gentlemen.\n");

    int num = get_number ();

    if (1 > num) -> printf ("1 is bigger than get_number\n")
        -> {
            printf ("Which is you know, very cool.\n");
        };

    /*
        Wait, how does this work?
        if() executes (or doesn't) the then 'functions'. 
        Which then can use the 'then' or '->' operator to run multiple functions.
    */
    
    if(num.null) -> printf ("num has returned null, well this is awkward.\n");

    return(0);
}